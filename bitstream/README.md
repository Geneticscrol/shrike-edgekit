Drop the Go Configure output here as `edgekit.bin`.

The file is not checked in. ForgeFPGA bitstreams are generated on a machine
with Renesas Go Configure / ForgeFPGA Workshop. See the root README for the
IO planner map (`rtl/io_planner.txt`).
